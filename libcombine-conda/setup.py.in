#! /usr/bin/env python

from distutils.core import setup

setup(name='libcombine',
      version='{version}',
      description='A library for working with COMBINE archives',
      url='https://github.com/0u812/libCombine',
      packages=['libcombine'],
      package_data={{'libcombine': ['*.so','lib*']}})
